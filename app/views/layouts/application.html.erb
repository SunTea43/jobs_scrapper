<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Job Scraper App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Job Scraper App">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
      <div class="container">
        <%= link_to "JobScraper 🚀", root_path, class: "navbar-brand fw-bold" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Jobs", jobs_path, class: "nav-link #{'active' if controller_name == 'jobs'}" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main>
      <%= yield %>
    </main>

    <footer class="footer mt-5 py-3 bg-white border-top text-center text-muted">
      <div class="container">
        <span>© <%= Time.now.year %> Job Scraper App - Built with Rails 8 & Python</span>
      </div>
    </footer>
  </body>
</html>
